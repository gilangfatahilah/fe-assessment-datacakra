import LoginForm from "@/components/form/Login";
import AuthFormLayout from "@/components/layout/Auth";

const LoginPage = () => {
  return (
    <AuthFormLayout>
      <LoginForm />
    </AuthFormLayout>
  );
};

export default LoginPage;
